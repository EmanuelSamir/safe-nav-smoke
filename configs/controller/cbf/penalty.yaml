# @package controller
# CBF — penalty mode (soft barrier in running cost, no hard projection)

safety:
  d_safe: 2.4
  r_sense: 8.0
  neighbor_mode: "nearest"

mppi:
  num_samples: 120
  horizon: 14
  lambda_: 3.0
  alpha_noise_sigma: 10.0
  noise_abs_cost: false
  device: "mps"

mode: "penalty"

k1: 1.5
k2: 1.5
L: 0.4
rho: 5.0
dt: ${env.clock}

