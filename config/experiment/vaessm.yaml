# @package experiment
defaults:
  - base

name: "vaessm_experiment"
type: "vaessm"

# VAESSM specific parameters
checkpoint_path: "${hydra:runtime.cwd}/checkpoints/vaessm_3rd.pt"
map_rule_type: "mean"
disable_risk_map: false

# Override base defaults
time_horizon: 10
samples: 20
