experiment_name: "fno"
seed: 42

data:
  data_path: "data/physics_smoke"

model:
  type: "fno"
  # Sobreescrituras para el modelo FNO si son necesarias
  # modes_t: 2

# Los demás valores usarán los defaults definidos en FNOTrainingConfig
